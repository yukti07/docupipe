-r requirements.txt

pytest==8.3.4
httpx==0.28.1

# The claim semantics genuinely cannot be mocked — two workers racing for one
# row is a database behaviour, not application logic, and a mock would test
# our belief about PostgreSQL rather than PostgreSQL.
testcontainers[postgres]==4.9.0
