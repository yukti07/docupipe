# Component Inventory

The canonical list of every UI component in Quarry, so that building a screen means *assembling*
rather than inventing.

Companion to [`screen-inventory.md`](screen-inventory.md) (what screens exist) and
[`ui-design-spec.md`](ui-design-spec.md) (what things look like).

---

## The rule

> **Reuse existing components wherever possible. Do not create a new component if an existing one
> can satisfy the requirement.**

Before writing any new component:

```
Need a piece of UI
      ↓
Is it in this inventory?
 ├── Yes → use it. If it's close but not exact, add a variant to it
 └── No
      ↓
 Can a shadcn/ui primitive do it?
 ├── Yes → install the primitive, don't hand-write one
 └── No
      ↓
 Can it be composed from two or three things already here?
 ├── Yes → compose in place. Only extract a component when it appears a third time
 └── No
      ↓
 Build it — and add it to this file in the same change
```

**The third-use rule.** Two similar bits of markup on two screens is a coincidence. Three is a
component. Extracting on the first sighting produces abstractions shaped by one caller, which then
fight the second one.

---

## Layers

| Layer | What it is | Who writes it | Lives in |
|---|---|---|---|
| **1 · Primitives** | shadcn/ui components | Generated by the CLI — **never hand-edited** | `src/components/ui/` |
| **2 · Common** | Generic pieces built from primitives. Nothing here knows what a document is | Us | `src/components/common/` |
| **3 · Domain** | Quarry-specific. These know about cells, evidence, batches, failures | Us | `src/components/quarry/` |

A layer only ever depends downward. Domain uses common uses primitives. Never the reverse.

---

## Layer 1 · Primitives

Install with `npx shadcn@latest add <name>`. If one of these is missing, install it rather than
writing your own.

| Component | Used for |
|---|---|
| `button` | Everywhere |
| `input` · `textarea` · `label` | Column editing, corrections, settings |
| `select` · `dropdown-menu` | Export format, density, row actions |
| `dialog` | Export, destructive confirmation — **and nothing else** |
| `sheet` | Evidence panel below 1024px |
| `resizable` | The table / evidence split |
| `tabs` | Results ↔ Machine view |
| `tooltip` | Supplementary hints only |
| `badge` | Base for `StatusBadge` |
| `progress` | Base for upload and batch progress |
| `separator` · `scroll-area` | Layout |
| `skeleton` | Table and panel loading |
| `sonner` | Toasts — confirmations only, never errors |
| `popover` | Column merge details, filter menus |
| `checkbox` · `switch` | Settings, row selection |
| `alert` | Base for `PausedBanner` and inline failures |

---

## Layer 2 · Common

Generic, reusable, and deliberately ignorant of the product's domain.

| ID | Component | Purpose | Variants / states | Screens |
|---|---|---|---|---|
| C01 | **`StatusBadge`** | The one badge in the app | `success` · `review` · `error` · `paused` · `neutral` | S01 S02 S04 S06 |
| C02 | **`EmptyState`** | Nothing here yet | With or without an action | S01 S06 S08 |
| C03 | **`ErrorState`** | Something failed at page level | With retry, with back | S01 S06 S12 S13 |
| C04 | **`LoadingState`** | Waiting, with words | `determinate` (has a fraction) · `labelled` (doesn't) | S01 S03 S04 S06 S07 |
| C05 | **`PageHeader`** | Title, subtitle, actions | Compact and full | S01 S02 S03 S10 |
| C06 | **`Toolbar`** | Row of controls above content | — | S06 |
| C07 | **`KeyHint`** | Shows a keyboard key — `A`, `Esc` | Single key, key sequence | S08 |
| C08 | **`ConfirmDialog`** | Confirm something costly | `default` · `destructive` | S06 S10 |

**`LoadingState` exists to make the no-bare-spinner rule structural.** If loading always goes
through a component that requires a label, it becomes impossible to ship a wordless spinner by
accident.

---

## Layer 3 · Domain

These know about Quarry.

### Upload — S02

| ID | Component | Purpose | States |
|---|---|---|---|
| D01 | **`DropZone`** | Drag target and file picker in one | `idle` · `dragging` · `dragging-invalid` · `disabled`. States the accepted formats and size cap inside itself |
| D02 | **`FileRow`** | One file and everything about it | `staged` · `checking` · `rejected` · `uploading` · `failed` · `done`. Owns its own retry |
| D03 | **`UploadList`** | The list of `FileRow`s, plus the summary | — |

### Columns — S03

| ID | Component | Purpose | States |
|---|---|---|---|
| D04 | **`ColumnProposalRow`** | One proposed column, editable in place | `detected` · `merged` · `edited` · `added` · `invalid` |
| D05 | **`ColumnList`** | The full proposal, reorderable | — |
| D06 | **`MergeDetail`** | Which files used which name (P1) | Inside a popover |
| D07 | **`DetectionCount`** | *"found in 47 of 50 files"* | — |

`DetectionCount` is tiny but deliberate: it's the only place a confidence figure is rendered, which
is what keeps an invented percentage from ever appearing.

### Processing — S04, S05

| ID | Component | Purpose | States |
|---|---|---|---|
| D08 | **`StatusSentence`** | The one honest line in the header | `running` · `paused` · `finished` · `failed` |
| D09 | **`DocumentStatusRow`** | One document and the stage it reached | Per-stage, plus `failed` |
| D10 | **`PausedBanner`** | Waiting on a limit, with the resume time | `quota` · `user-cap` |
| D11 | **`StageColumn`** | One processing stage with its documents (P1) | `idle` · `active` |
| D12 | **`ProviderChainStrip`** | The provider chain, one link greyed (P1) | `live` · `spent` · `unavailable` |

### Results — S06

| ID | Component | Purpose | States |
|---|---|---|---|
| D13 | **`DataTable`** | The TanStack wrapper — sort, filter, virtualize, density | `loading` · `loaded` · `empty` · `filtered-empty` |
| D14 | **`DataCell`** | **The most important component in the app** | `value` · `not-found` · `marked` · `corrected` · `failed-row` |
| D15 | **`BatchCard`** | A batch on the workspace | `running` · `paused` · `finished` · `finished-with-failures` · `failed` |
| D16 | **`DensityToggle`** | Comfortable ↔ compact | — |
| D17 | **`ExportDialog`** | Format choice and the honest counts | `idle` · `warning` · `preparing` · `done` · `failed` |

### Evidence — S07

| ID | Component | Purpose | States |
|---|---|---|---|
| D18 | **`EvidencePanel`** | The shell — header, source name, close | `closed` · `opening` · `open` · `loading` · `unavailable` |
| D19 | **`EvidenceView`** | Dispatches on locator type to the right view below | — |
| D20 | **`EvidencePageView`** | pdf.js page render + highlight — PDF, scan, image | `rendering` · `rendered` |
| D21 | **`EvidenceAudioView`** | Player cued to the moment, with the words | `loading` · `ready` · `playing` |
| D22 | **`EvidenceTextView`** | Paragraph with the phrase highlighted | — |
| D23 | **`EvidenceRecordView`** | Sheet, row and column | — |
| D24 | **`EvidenceUnavailable`** | This format can't report a position — say so, show the text | — |
| D25 | **`HighlightBox`** | The box itself, and the drawing animation | `drawing` · `drawn` · `editable` (used when correcting by box) |

**One interface, five views.** Every adapter produces the same kind of locator, so `EvidenceView`
switches on its type and nothing above it needs to know which format it's looking at. Adding a
format later means adding one view, not touching the panel.

### Review — S08 (P1)

| ID | Component | Purpose | States |
|---|---|---|---|
| D26 | **`ReviewCard`** | One marked cell: value, reason, evidence, actions | `showing` · `correcting` · `drawing` · `saving` · `saved` |
| D27 | **`ReviewQueue`** | The loop, the progress, the keyboard handling | `active` · `empty` · `done` |
| D28 | **`ReviewReason`** | Why this cell is marked, in plain words | One per verification rule |

### Cross-cutting

| ID | Component | Purpose | Notes |
|---|---|---|---|
| D29 | **`FailureMessage`** | **Turns a failure class into a sentence and a next step** | The single place a failure becomes text |
| D30 | **`ConnectionStatus`** | Live updates lost, reconnecting, offline | Global |
| D31 | **`WakingUpState`** | The cold-start pause, named (P1) | Wraps `LoadingState` |

**`FailureMessage` is the one to get right.** Every failure class maps to its copy in exactly one
component. That's what makes "no raw error string ever reaches the user" a structural property
rather than a thing everyone has to remember. If a screen renders `error.message` directly, that's
a bug regardless of how good the message happens to look.

---

## Naming

- **PascalCase**, file named for the component: `DataCell.tsx`
- **No `Quarry` prefix.** The folder already says it
- **No abbreviations.** `EvidencePanel`, never `EvPanel`
- **Say the thing, not the shape.** `PausedBanner`, not `YellowBox`
- Domain components are named for what they *mean*, not where they sit — `DataCell` works in the
  table and in review because it isn't called `TableCell`

---

## Do not create these

Common instincts that would break the system.

| Don't | Use instead |
|---|---|
| A second badge or pill | `StatusBadge` with a new variant |
| A bare spinner anywhere | `LoadingState` — it requires a label |
| A custom modal | `dialog`, or `sheet` on small screens |
| A toast for an error | `ErrorState` or `FailureMessage` — errors persist, toasts don't |
| A one-off cell renderer | `DataCell` with a new state |
| Rendering `error.message` directly | `FailureMessage` |
| A second table | `DataTable` with different columns |
| A bespoke evidence layout for a new format | A new `EvidenceView` case |
| Your own colour values | The tokens in [`ui-design-spec.md`](ui-design-spec.md) |

---

## Adding to this inventory

When a genuinely new component is needed:

1. Confirm nothing above covers it, including as a variant
2. Confirm it's earned its place — a third occurrence, or it's clearly domain-shaped
3. Place it in the right layer, and check it only depends downward
4. Name it for meaning
5. **Add it to this file in the same change as the code.** An inventory that lags the codebase is
   worse than none, because it gets trusted and it's wrong

---

## Build order

Roughly the order these become necessary, which is also roughly the safest order to build them.

```
1. Primitives + StatusBadge, LoadingState, EmptyState, ErrorState, PageHeader
2. DropZone, FileRow, UploadList                         → S02 works
3. ColumnProposalRow, ColumnList, DetectionCount         → S03 works
4. StatusSentence, DocumentStatusRow, PausedBanner       → S04 works
5. DataTable, DataCell, BatchCard                        → S06 and S01 work
6. EvidencePanel, EvidenceView, EvidencePageView,
   HighlightBox, EvidenceUnavailable                     → the product works
7. FailureMessage everywhere it belongs                  → P0 complete
8. ReviewCard, ReviewQueue, ReviewReason, KeyHint        → P1 begins
```

Step 6 is the one that must not be deferred. Everything before it is a table, and a table isn't the
product.
